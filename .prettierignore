# Build output — never format generated files
.next/
out/

# Static assets — binary files and vendor JS have no business being reformatted
public/

# Node.js scripts — plain ESM, not part of the Next.js/TypeScript pipeline
scripts/
